{
    "name": "kader",
    "private": true,
    "scripts": {
        "build": "bun x turbo build",
        "build:server": "bun x turbo build --filter=\"@kader/server\"",
        "dev": "bun x turbo dev",
        "lint": "bun x turbo lint",
        "format": "prettier --write \"**/*.{ts,tsx,md}\"",
        "start:server": "node ./apps/server/dist/index.js"
    },
    "devDependencies": {
        "prettier": "^3.2.5",
        "turbo": "^2.0.14",
        "typescript": "^5.4.5"
    },
    "engines": {
        "node": ">=18"
    },
    "packageManager": "bun@1.1.22",
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "dependencies": {
        "drizzle-orm": "^0.33.0"
    }
}
